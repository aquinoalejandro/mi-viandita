import { StyleSheet } from "react-native";
import { palette } from "../../theme/appTheme";

export const miViandaDialogStyle = StyleSheet.create({
  dialog: {
    borderRadius: 10,
    overflow: "hidden",
    backgroundColor: palette.surface,
  },
}).dialog;
